Changelog
1.  Extend FieldArray to QuantFieldArray
    -   QuantFieldArray already in some of the examples (should be part of FlexUnits)
    -   QuantFieldArray{N,T,L} <: FieldArray{N,T,L}
        -   Added DimsMod{D, QA<:QuantFieldArray} to allow for modified dimension behaviour
    -   Constructors will convert quantities to dimensions (dimtype(fieldtype(fn))()/D())

2.  Introduced DimsMod{D}(qa::QuantFieldArray)
    -   Modifies a QuantFieldArray by a dimension D 
    -   Useful for taking derivatives

3.  Fixed issues with converting dimensionless LogQuant to a numeric value

Backlog:

(future)
0.  Extend and clean out tests 
    -   Include simulation cases in tests (simply test the flag of the simulation output)
        -   Test Raw / Dynamic / QuantFieldArray versions of FallingObject 
        -   Test Raw / QuantFieldArray versions of Stiction
        -   These two cases will be separate "TestItem" objects 
            -   Tests within these two items are separate "TestSet" objects
    -   Move unused test code to an "Archives" folder


1.  Add compatibility layer with Dates 
    -   Create "seconds_unit(::Type{D}) where D<:AbstractDimensions" function (and extend for Units, and StaticDims)
        -   This should return a StaticDims or Unit{StaticDims}
        -   This usually returns StaticDims{D(s=1)}
    -   Define toseconds(p::Period)
    -   Define conversions
        -   convert(::Type{Q}, p::Period) = convert(Q, quantity(toseconds(p), seconds_unit(dimtype(Q))))
    -   Define rounding functions and conversion to Period

2.  Add QuantFieldArray section to LinearAlgebra docs

3.  Move factorizations to LinearAlgebra extension
    -   implement a unitful "cholupdate!" and "choldowndate!"
    -   Look at UnitfulLinearAlgebra, and UnitfulTensors for other factorizations
    -   Add performance benchmarks

4.  Add plot recipes
    -   https://github.com/JuliaPlots/Plots.jl/blob/v2/PlotsBase/ext/UnitfulExt.jl



#Example of #3 is shown below

julia> using FlexUnits, .UnitRegistry

julia> (x, y, z) = 1u"m/s", 1u"kg", 1u"kW"
(1.0 m/s, 1.0 kg, 1000.0 (m² kg)/s³)

julia> T = Union{typeof(x),typeof(y), typeof(z)}
Union{Quantity{Float64, StaticDims{m/s}}, Quantity{Float64, StaticDims{kg}}, Quantity{Float64, StaticDims{(m² kg)/s³}}}

julia> v = T[x,y,z]
3-element Vector{Union{Quantity{Float64, StaticDims{m/s}}, Quantity{Float64, StaticDims{kg}}, Quantity{Float64, StaticDims{(m² kg)/s³}}}}:
 1.0 m/s
 1.0 kg
 1000.0 (m² kg)/s³

julia> map(x->x*2, v)
3-element Vector{Quantity{Float64}}:
 2.0 m/s
 2.0 kg
 2000.0 (m² kg)/s³

julia> map(x->x*2, [x,y,z])
3-element Vector{Quantity{Float64, Dimensions{FixRat32}}}:
 2.0 m/s
 2.0 kg
 2000.0 (m² kg)/s³

julia> collect(map(x->x*2, (x,y,z)))
3-element Vector{Quantity{Float64}}:
 2.0 m/s
 2.0 kg
 2000.0 (m² kg)/s³

julia> vcat((map(x->x*2, (x,y,z)))...)
3-element Vector{Quantity{Float64, Dimensions{FixRat32}}}:
 2.0 m/s
 2.0 kg
 2000.0 (m² kg)/s³





#====================================================================================================
Some benchmarks
====================================================================================================#
import DynamicQuantities
import Unitful
using BenchmarkTools

v1flex = ubase.([1u"m/s", 1u"J/kg", 1u"A/V"])
v1uni  = [1*Unitful.u"m/s", 1*Unitful.u"J/kg", 1*Unitful.u"A/V"]
v1dyn  = [1*DynamicQuantities.u"m/s", 1*DynamicQuantities.u"J/kg", 1*DynamicQuantities.u"A/V"]

@btime sum(x->x^0.0, v1uni)
@btime sum(x->x^0.0, v1flex)
@btime sum(x->x^0.0, v1dyn)

t1flex = ubase.((1u"m/s", 1u"J/kg", 1u"A/V"))
t1uni  = (1*Unitful.u"m/s", 1*Unitful.u"J/kg", 1*Unitful.u"A/V")
t1dyn  = (1*DynamicQuantities.u"m/s", 1*DynamicQuantities.u"J/kg", 1*DynamicQuantities.u"A/V")

@btime sum(x->x^0, t1uni)
@btime sum(x->x^0, t1flex)
@btime sum(x->x^0, t1dyn)


